name: offline_ai_app
description: Offline AI app with text and image generation
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  workmanager: ^0.5.2
  image: ^4.1.3
  http: ^1.1.0

flutter:
  uses-material-design: true